✅Aditi			Backend (Supabase- Created project named DawaTrack, using its own SQL editor created the tables medicine_expiry, prescription_medicines, checkup, profiles, to store user data. Applied the RLS policies to every table to maintain security and privacy. Under Authentication, it stores the number of active users with columns such as unique user id creation, creation time of account and the last sign in date and time, for future, we will be including the google sign in feature, Under Authentication only, added URL configuration which has our main site URL and redirect URL's

✅Adit			Plan B

✅Abhinandan		Backend (Google API- created google calendar API key form API library a cloud console provided by the Google, Authorized URL and re-direct URL's, Made the project available for all audience, provided client ID and client secret to the script.js, Also tried for google verification, but as we don't have a domain on our own name its still pending, just google accepted our privacy policy, in future we will be again using google's API for location (google maps) in our service under checkup reminder feature)

✅Abhishek P		Frontend- the login/sign up page, the main page/home page of our website, the format of all three features, the search box and the user profile contents. Email Service- created an edge function from supabase, edge function is basically a provision provided by the supabase to perform a particular function which it is not by default built in for example sending of emails(reminders/notification), so created a send-email deno.json file which will integrate with the supabase edge functions and the frontend means if the user saved a reminder the function will be deployed and confirmation email is sent. Created privacy policy

✅Abhishek S		Frontend-Styles (css file is of approx 2000+ lines so you have to explain in detail about each style. Animations, smooth transitions, color scheme of website, notification messages shown on the website itself, sorted them based on the notification like if it is a success message then green color scheme if error then red, if a warning then yellow)

✅Pradyumna		Supabase- Email template created with confirm button which redirect to email-confirmed.html if button doesn't work we have added a link which acts same as button, which has a button to return to login. Also created a bucket in supabase for storage of prescription images, based on their unique id generated when profile saved. When users ask for their prescription images by contacting us, we can even provide the link to them via emails.